#include <stdio.h>
#include "config.h"
#include <string.h>

int main( int argc, const char* argv[] ){
	printf("kbotpi version %u.%u\n", kbotpi_VERSION_MAJOR, kbotpi_VERSION_MINOR);
  
  for(int i=0; i < argc; i++){
		if(strcmp(argv[i], "--help")){

		}

	
	}
}
